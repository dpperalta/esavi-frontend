// Placeholder — SPEC FE01 paso 3 lo reemplaza por la comparación real de claves
// entre src/locales/{es,en,nl}.json.
console.log('i18n:check — todavía no hay locales que comparar (SPEC FE01, paso 3)');
process.exit(0);
