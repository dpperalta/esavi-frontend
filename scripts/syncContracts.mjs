// Placeholder — SPEC FE01 paso 5 lo reemplaza por la copia real desde
// ../esavi-backend/src/types hacia src/contracts/.
console.log('contracts:sync — todavía no hay nada que sincronizar (SPEC FE01, paso 5)');
process.exit(0);
